1. Configure connectivity:
    * VPC: Select your VPC
    * Subnet group: Create or select existing
    * Public accessibility: For testing, enable it (disable in production)
2. Configure additional settings:
    * Database name: testdb
    * Backup retention: 7 days
    * Backup window: Specify time
    * Enable encryption: Yes
3. Click "Create database"
Part 2: Modify Instance
1. Once instance shows "available" status
2. Click instance identifier
3. Modify settings like instance class, storage, backup retention
4. Apply changes immediately or during maintenance window
Part 3: Monitor Performance
1. Navigate to CloudWatch metrics section
2. Observe CPU utilization, database connections, storage space
3. Review database logs in Logs & events tab


_______________________________________

-- Basic table--
CREATE TABLE employees (
    id INT PRIMARY KEY AUTO_INCREMENT,
    first_name VARCHAR(50),
    last_name VARCHAR(50),
    email VARCHAR(100),
    salary DECIMAL(10, 2),
    hire_date DATE
);

-- Table with more features--
CREATE TABLE products (
    product_id INT PRIMARY KEY AUTO_INCREMENT,
    product_name VARCHAR(100) NOT NULL,
    category VARCHAR(50),
    price DECIMAL(8, 2) NOT NULL,
    stock_quantity INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- Table with foreign key (relationship)
CREATE TABLE orders (
    order_id INT PRIMARY KEY AUTO_INCREMENT,
    employee_id INT,
    order_date DATE,
    total_amount DECIMAL(10, 2),
    FOREIGN KEY (employee_id) REFERENCES employees(id)
);
______________________
-- Show all tables in current database
SHOW TABLES;

-- Show table structure
DESCRIBE employees;
-- Or shorter version:
DESC employees;

-- Show table creation statement
SHOW CREATE TABLE employees;

-- Show table size
SELECT 
    TABLE_NAME, 
    ROUND(((data_length + index_length) / 1024 / 1024), 2) AS size_mb
FROM information_schema.TABLES 
WHERE TABLE_SCHEMA = 'company_db';
